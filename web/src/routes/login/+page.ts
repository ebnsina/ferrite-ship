// Client-only: the page asks the control plane whether an account exists, and
// there is nothing meaningful to prerender before that answer arrives.
export const prerender = false;
export const ssr = false;
